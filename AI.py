
from model import *

class AI:
    def __init__(self, currentState):
        message = None # type: str
        direction = None # type: DIRECTION
        self.currentState = currentState

# this will be called each turn and it should return a message and a direction
    def turn(self):

    	return {
    		message,
    		direction,
    	}
